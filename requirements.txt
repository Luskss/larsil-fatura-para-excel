# Dependências Python para o servidor OCR local (ocr_server.py)
#
# Instale com:
#   pip install -r requirements.txt
#
# Nota: paddlepaddle 2.6.2 evita o bug do executor PIR presente no Paddle 3.x.
# Os modelos PaddleOCR são baixados automaticamente na primeira execução.

flask>=3.0
paddlepaddle==2.6.2
paddleocr==2.7.3
pymupdf>=1.24
numpy>=1.24,<2.0
opencv-python
